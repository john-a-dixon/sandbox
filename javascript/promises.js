// Function to demonstrate promises
const mockWait = (message, time) => {
  setTimeout(() => console.log(message), time);
}

mockWait("Test", 1000)
// Standard promise

// Chained promise

// Promise all