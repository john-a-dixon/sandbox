//-------------------------------------------------------------------------FUNCTION DECLARATIONS

// Standard function declaration
function funcInfo(x) {
  const info = `Parameter = x\nArgument = "${x}"\nIdentifier = funcInfo`;
  console.log(info);
}

//-------------------------------------------------------------------------FUNCTION CALLS
funcInfo("Arg")