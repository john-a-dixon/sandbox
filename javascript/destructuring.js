// Destructuring with arrays
let alpha = ["a", "b", "c"];
let [a, b, c] = alpha
console.log(a, b, c);

const [first] = alpha;
console.log(first);

// Destructuring with objects

// Destructuring with functions