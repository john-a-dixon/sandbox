const assert = require("assert");
const { altCase } = require("./app.js");

describe("Casing", () => {
  describe(".altCase", () => {
    it("returns alternating casing for a word", () => {

    });
  });
});